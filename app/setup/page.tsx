import { House, ArrowLeft } from 'lucide-react';
export default function SetupPage(){return <main className="setup-page"><a className="text-link" href="/app"><ArrowLeft size={16}/>Back to household</a><h1>Free email reminders, no domain needed.</h1><p>A small Google Apps Script sends cleaning reminders from your Gmail account. It also asks the app to delete expired photos. It runs in Google’s cloud, so your computer can be switched off.</p><section className="card"><h2>One-time setup</h2><ol><li>Sign in to the Gmail account you want to send from and open <a href="https://script.google.com/home/start" target="_blank" rel="noreferrer">Google Apps Script</a>. Create a new project called “Housekeeping reminders”.</li><li>Copy the contents of <a href="/email-reminders.gs" download>this reminder script</a> into the script editor, replacing the example code. Save it.</li><li>In <b>Project Settings → Script properties</b>, add <code>APP_URL</code> with this app’s production HTTPS address, and <code>WORKER_SECRET</code> with the same secret configured in Vercel. Copy the secret from your local <code>.env.local</code> file and keep the script private.</li><li>Choose the <code>install</code> function and click <b>Run</b>. Review and authorize Google’s permission prompt. The script sends emails; it does not read your inbox.</li><li>Run <code>tick</code> once. Check the execution log, then return to Household settings. “Reminder connection” should say “Connected”.</li></ol><p>The script runs about every 15 minutes. Cleaning dates and reminders use Melbourne time automatically, including daylight saving.</p></section><section className="card"><h2>What’s free?</h2><p>Google currently allows 100 email recipients per day for personal Apps Script accounts. Your household usually needs only a few. You do not need to buy a domain or provide your Gmail password.</p><p>Reminders go only to the six approved household email addresses.</p><p><a href="https://developers.google.com/apps-script/guides/services/quotas" target="_blank" rel="noreferrer">Google’s current quotas</a></p></section><section className="card"><h2>Keep it running</h2><p>Don’t delete the script or revoke its permission while the household is using it. If a run fails, Google shows the failure in Executions and the admin dashboard shows the last connection status. Missed runs retry on the next trigger.</p><p>Photos expire seven days after upload. Physical removal happens on the next successful cleanup run. Cleaning records remain in the app.</p></section></main>;}
